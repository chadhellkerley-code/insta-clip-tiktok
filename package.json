{
  "name": "insta-clip-tiktok",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cd web-app && npm run dev",
    "build": "cd web-app && npm run build",
    "start": "cd web-app && npm run start",
    "lint": "cd web-app && npm run lint",
    "install-all": "npm install && cd web-app && npm install"
  }
}
